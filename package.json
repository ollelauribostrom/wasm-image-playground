{
  "name": "wasm-image-playground",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.1.1",
    "shortid": "^2.2.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "now-clean": "now rm wasm-image-playground -y",
    "now-setup": "cp now.json ./build/now.json && cd ./build",
    "now": "npm run now-clean && npm run build && npm run now-setup && now && now alias"
  }
}
